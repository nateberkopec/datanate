{
  "name": "datanate",
  "version": "0.0.1",
  "description": "Personal metrics dashboard with static site generation",
  "private": true,
  "scripts": {
    "install-deps": "npm install"
  },
  "dependencies": {
    "d3": "^7.9.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=16.0.0"
  }
}
